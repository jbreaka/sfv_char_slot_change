package jbreaka.io

import java.io.File

class FileManager {
  def manage(currSlot:String, newSlot:String, files:Set[File])={
    renameIndividualFiles(currSlot, newSlot, files)
  }

  def renameIndividualFiles(currSlot:String, newSlot:String, files:Set[File])={

  }
}
